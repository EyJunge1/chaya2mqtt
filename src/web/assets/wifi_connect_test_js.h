#pragma once

#include <Arduino.h>

/** Poll /wifi-connect-status during AP Wi-Fi test; on ok POST commitForm for reboot + fallback redirect after delay. Minified IIFE. */
inline const char WIFI_CONNECT_TEST_JS[] PROGMEM =
    R"wc((function () {var st = document.getElementById('st'); var committed = false; var pollStopped = false; var kPollMs=2000;var kRedirectDelayMs=10000;var origin=(typeof window.__CHAYA_HTTP_ORIGIN__==='string'&&window.__CHAYA_HTTP_ORIGIN__)?window.__CHAYA_HTTP_ORIGIN__:(location.origin+'/'); function poll() {if (pollStopped) {return;} fetch('/wifi-connect-status').then(function (r) {return r.json();}).then(function (d) {if (!d || !d.state) {st.textContent = 'Status error.'; return;} if (d.state === 'idle') {window.location.href = '/wifi'; return;} if (d.state === 'testing') {st.textContent = 'Testing connection to ' + (d.ssid || '') + '…'; return;} if (d.state === 'fail') {st.textContent = 'Could not connect to ' + (d.ssid || '') + '.'; var fa = document.getElementById('failActions'); if (fa) {fa.style.display = 'block';} return;} if (d.state === 'ok') {st.textContent = 'Connected! Saving and rebooting…'; if (!committed) {committed = true; pollStopped = true; setTimeout(function () {window.location.href = origin;}, kRedirectDelayMs); var f = document.getElementById('commitForm'); if (f) {f.submit();}} return;} }).catch(function () {if (!pollStopped) {st.textContent = 'Status error.';}}); } setInterval(poll, kPollMs); poll(); })();)wc";
