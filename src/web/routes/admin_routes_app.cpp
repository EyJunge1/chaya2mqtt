#include <Arduino.h>

#include "admin_routes.h"

// Application HTML routes were replaced by the React SPA (`admin_routes_spa.cpp`)
// and JSON API (`admin_routes_api.cpp`).

void adminRoutesRegisterApplication(AsyncWebServer& ws) {
    (void)ws;
}
