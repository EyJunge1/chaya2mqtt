version: 2

# PlatformIO (platformio.ini / lib_deps) is not yet supported by Dependabot.
# See: https://github.com/dependabot/dependabot-core/issues/13206
# PlatformIO libraries, platform/framework pins, and the pip PlatformIO CLI pin
# are updated by Renovate (renovate.json), grouped by update type.

updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Etc/UTC
    open-pull-requests-limit: 3
    labels:
      - dependencies
    groups:
      github-actions:
        patterns:
          - "*"
        update-types:
          - minor
          - patch
      github-actions-major:
        patterns:
          - "*"
        update-types:
          - major

  - package-ecosystem: npm
    directory: /frontend
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Etc/UTC
    open-pull-requests-limit: 5
    labels:
      - dependencies
    # ESLint 10 / typescript-eslint do not support TypeScript 7 yet (peer: <6.1.0).
    ignore:
      - dependency-name: "typescript"
        versions: [">=7"]
        update-types: ["version-update:semver-major"]
    groups:
      frontend-minor-patch:
        patterns:
          - "*"
        update-types:
          - minor
          - patch
      frontend-major:
        patterns:
          - "*"
        exclude-patterns:
          - "typescript"
        update-types:
          - major

  - package-ecosystem: npm
    directory: /flasher
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Etc/UTC
    open-pull-requests-limit: 5
    labels:
      - dependencies
    # ESLint 10 / typescript-eslint do not support TypeScript 7 yet (peer: <6.1.0).
    ignore:
      - dependency-name: "typescript"
        versions: [">=7"]
        update-types: ["version-update:semver-major"]
    groups:
      flasher-minor-patch:
        patterns:
          - "*"
        update-types:
          - minor
          - patch
      flasher-major:
        patterns:
          - "*"
        exclude-patterns:
          - "typescript"
        update-types:
          - major
